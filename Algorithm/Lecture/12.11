강조 목록:
말단노드의 갯수
결정된 것은 2진트리
노드의 높이 2^?

말단단이 몇개의 노드가 되려면 깊이가 얼마보다 깊어야한다.
어떤 것은 5번, 6번 정렬됨?
과연 팩토리얼에 대해서는 과연 몇번인가요? 6번

검색
운이 좋으면 1번 운이 나쁘면 n-1번 비교해야함
decison트리도 2진트리임
searching은 전체 트리의 개수는 적어도 n개는 되어야함
길이 n개로 쌓을 때는 log(n)개의 깊이 -> 정렬은 nlog(n)

최고 높은 수가 이긴 수들 중에서 차대키가 있다.
54page의 15와 16을 이겼기 때문에 둘 중에 하나가 차대값이다.

실행 개수가 7이고 깊이가 3이다. log(7)는 차대키 리스트 개수 -> log(n)-1 횟수 (최대값 찾는 시간)
n-1번은 최대값구하기 방법
n-1+log(n)-1 = n+log(n)-2


k번째 작은 키 찾기
1. 단순 정렬 후 탐색: O(nlog(n))
2. partition 사용 : quick sort 사용
    wosrt       : n(n-1)/2
    best        : 2n
    average     : 3n
