함수는 매개변수 여러개이지만 반환값은 하나


코드를 받았을 때 실행하는 과정은 레지스터에 등록된 코드가 순차적으로 인스트럭션이
프로그램카운터가 하나하나 증가하는 과정을 가지게 됨
하지만 함수가 있다면 위치가 함수의 위치로 움직임


caller 호출자
callee 생성자

stack
변수
함수를 실행하기 위해 받아오는 매개변수 (int x) 
    -> 이런것도 위치가 필요하다.
함수로 돌아오기 위한 주소 -> 만약 함수로 복귀한다면 해당 영역은 삭제

함수
만약 main이 끝나면 stack의 모든 데이터를 날림. -> 시스템 종료
heap




ex) 11page:


stack
sqr함수의 크기만큼만 만들면 됨 4byte -> 리턴값은 레지스터에 저장해서 반환함. / 레지스터는 비싸기 때문에 하나로 뭉쳐서 반환(리턴이 하나인 이유)




main();
heap
